import 'package:flutter/material.dart';

class AppMonth extends StatelessWidget {
  const AppMonth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
